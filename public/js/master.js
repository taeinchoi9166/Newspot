document.addEventListener('DOMContentLoaded',function(){
    let dateTag = document.querySelector('.datepicker');
    console.log(M);
    let dateCalander = M.Datepicker.init(dateTag,{format:'yyyy-mm-dd'});

});